import { ComponentMeta } from "@storybook/react";
import ResetIcon from "../Reset";

export default {
  title: "Common/Reset",
  component: ResetIcon,
} as ComponentMeta<typeof ResetIcon>;

export const Reset = () => <ResetIcon />;
