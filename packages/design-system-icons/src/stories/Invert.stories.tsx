import { ComponentMeta } from "@storybook/react";
import InvertIcon from "../Invert";

export default {
  title: "Common/Invert",
  component: InvertIcon,
} as ComponentMeta<typeof InvertIcon>;

export const Invert = () => <InvertIcon />;
