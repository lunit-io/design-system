import SvgIcon, { SvgIconProps } from "@mui/material/SvgIcon";

{{#variants}}
import {{variant}} from "../../assets/ic_{{name}}={{variant}}_{{size}}px.svg";
{{/variants}}

const variants = [{{#variants}}'{{variant}}', {{/variants}}] as const;
type Variant = typeof variants[number];
type IconProps = SvgIconProps & {
  variant?: Variant;
};
{{#isDeprecated}}
/**
 * @deprecated
 */
{{/isDeprecated}}
const {{componentName}} = ({variant = 'outlined', ...rest}: IconProps) => {
  {{#variants}}
  if (variant === '{{variant}}') {
    return <SvgIcon {...rest} component={ {{variant}} } inheritViewBox />;
  }
  {{/variants}}

  return null;
};

{{componentName}}.displayName = '{{componentName}}';
{{componentName}}.variants = variants;

export default {{componentName}};
